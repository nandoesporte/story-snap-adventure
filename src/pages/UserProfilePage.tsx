
import React from "react";
import Profile from "./Profile";

// UserProfilePage is a wrapper around the existing Profile page
const UserProfilePage = () => {
  return <Profile />;
};

export default UserProfilePage;
